[build-system]
requires = ["setuptools>=50.3.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.coverage.run]
branch = true
source = ["hathizip"]
relative_files = true

[tool.coverage.paths]
source = ["hathizip"]

[tool.coverage.report]
exclude_lines =[
    "if TYPE_CHECKING:"
]
